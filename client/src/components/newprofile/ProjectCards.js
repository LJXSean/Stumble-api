import React from 'react';
import PropTypes from 'prop-types';

const ProjectCards = (props) => {
  return <div style={{ backgroundColor: 'black' }}>ProjectCards</div>;
};

ProjectCards.propTypes = {};

export default ProjectCards;
